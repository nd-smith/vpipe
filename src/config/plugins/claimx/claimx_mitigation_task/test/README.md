# Test Output Directory

This directory contains test output files generated when running the mitigation tracking worker in dev/test mode.

Files in this directory (except this README) are ignored by git.
